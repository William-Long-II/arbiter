## Intent
Source: pr-body
Title: Add widget factory

This PR adds a factory function for creating widgets.

## Pull Request acme/widget#7
Title: Add widget factory
Changed files: 2 (+15 / -2)

### PR description
This PR adds a factory function for creating widgets.

## Test coverage signal
added_source_lines: 15
added_test_lines: 0
untested_new_symbols:
- src/factory.ts :: createWidget
note: symbol list is regex-derived and may miss dynamic exports or destructured assignments.

## Language hints
Look for these language-specific patterns in the diff. Do not comment if they are not present.
- **Unhandled promise rejection**: Check for `async` functions whose return value is never `await`ed or `.catch()`ed (floating promises). A fire-and-forget call that rejects silently can crash the Node process or lose errors.
- **`any` in new exported signatures**: Look for `any` in newly added exported function parameters or return types. Using `any` in a public API defeats TypeScript's type-safety guarantee for all callers and is rarely intentional.
- **Non-null assertion (`!`) on externally sourced data**: Review `!` assertions applied to values that come from API responses, environment variables, or user input. These are the exact cases where `null`/`undefined` can legitimately occur at runtime.
- **Unsafe `as` cast narrowing a union**: Flag `as SomeType` casts that narrow a union (e.g. `value as string` when `value` is `string | null`). This silences the compiler but can cause runtime errors if the assumption is wrong. Prefer a type guard or explicit narrowing.
- **Mutated or object-valued default parameter**: In JavaScript/TypeScript, default parameter values that are objects or arrays are re-evaluated each call, unlike Python. Watch for code that mutates the default value or relies on identity across calls.

## Diff

### src/factory.ts (added, +12 / -0)
```diff
@@ -0,0 +1,12 @@
+export type Widget = {
+  id: string;
+  name: string;
+};
+
+export function createWidget(name: string): Widget {
+  return {
+    id: Math.random().toString(36).slice(2),
+    name,
+  };
+}

```

### src/index.ts (modified, +3 / -2)
```diff
@@ -1,3 +1,4 @@
-// placeholder
+export { createWidget, type Widget } from './factory';
+export { run } from './runner';

```