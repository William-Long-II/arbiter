# reviewme

Greenfield rewrite in progress.

The previous version of this project (Bun + TypeScript + SQLite) is preserved
on the `archive/v1` branch. The architecture and stack for the rewrite are
being redesigned from scratch.
